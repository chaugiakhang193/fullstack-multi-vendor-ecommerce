import NotFoundView from '@/components/shared/not-found-view';

// 404 trong nhóm (customer) — dùng chung giao diện với root not-found.
export default function CustomerNotFound() {
  return <NotFoundView />;
}
