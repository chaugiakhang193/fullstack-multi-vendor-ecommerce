{
  "name": "fullstack-web-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Repo-root dev tooling only (husky + lint-staged). App code lives in backend/, frontend/ and notification-service/.",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "{frontend,backend,notification-service}/**/*.{ts,tsx,css}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.8.4"
  }
}
