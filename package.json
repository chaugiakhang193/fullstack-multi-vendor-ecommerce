{
  "name": "fullstack-web-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Repo-root dev tooling only (husky + lint-staged). App code lives in backend/ and frontend/.",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "{frontend,backend}/**/*.{ts,tsx,css}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.8.4"
  }
}
